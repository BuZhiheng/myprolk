<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/color_green</item>
        <item name="colorPrimaryDark">@color/color_green</item>
        <item name="colorAccent">@color/color_green</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="textAllCaps">false</item>
    </style>
    <style name="SwipeBackTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/color_green</item>
        <item name="colorPrimaryDark">@color/color_green</item>
        <item name="colorAccent">@color/color_green</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="textAllCaps">false</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
    <style name="PopwinTheme" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/popwin_share_enter</item>//进入时的动画
        <item name="android:windowExitAnimation">@anim/popwin_share_exit</item>//退出时的动画
    </style>
    <style name="NewActivityDialog" parent="Theme.AppCompat.Dialog">
        <!-- 背景颜色及透明程度  -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否没有标题  设置之后虚拟键盘会和布局重合-->
        <!--<item name="windowNoTitle">true</item>-->
        <!-- 是否浮现在activity之上  -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否模糊  -->
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>